{
  "name": "retirement-calculator",
  "version": "1.0.0",
  "description": "",
  "main": "lib/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "docker build -f docker/Dockerfile -t calc . && docker run calc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jonathan Li",
  "license": "UNLICENSED",
  "dependencies": {
    "typescript": "^2.7.2"
  },
  "devDependencies": {
    "@types/yargs": "^11.0.0",
    "yargs": "^11.0.0"
  }
}
